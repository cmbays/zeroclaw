/target
firmware/*/target
*.db
*.db-journal
.DS_Store
._*
.wt-pr37/
__pycache__/
*.pyc
docker-compose.override.yml
site/node_modules/
site/.vite/
site/public/docs-content/
gh-pages/
.idea

# Environment and secret files — never commit actuals, only *.example variants
.env
.env.*
*.env
!.env.example
!*.example.env
.envrc.mattermost
infra/mattermost/.env

# Secret keys and credentials
.secret_key
*.key
*.pem
credentials.json

# Python virtual environments
.venv/
venv/

# ESP32 build cache (esp-idf-sys managed)
.embuild/

# SQLite memory databases (may contain conversation history)
*.sqlite
*.sqlite3

# Fork live configs (contain tokens — templates/examples are committed, actuals are not)
config/config.toml
config/config.mattermost.toml
config/*.toml
!config/*.example.toml
.worktrees/

# Nix
result