/target
firmware/*/target
*.db
*.db-journal
.DS_Store
._*
.wt-pr37/
__pycache__/
*.pyc
docker-compose.override.yml

# Environment files (may contain secrets)
.env
.env.fork

# Fork config (contains Slack/Linear secrets — never commit)
config/config.toml

# Python virtual environments

.venv/
venv/

# ESP32 build cache (esp-idf-sys managed)

.embuild/
.env.local
.env.*.local

# Secret keys and credentials
.secret_key
*.key
*.pem
credentials.json
.worktrees/

# Mattermost Hub secrets (templates are committed, actuals are not)
infra/mattermost/.env
.envrc.mattermost
config/config.mattermost.toml
config/sokka.toml
config/katara.toml
config/toph.toml
config/azula.toml
config/iroh.toml
config/aang.toml
