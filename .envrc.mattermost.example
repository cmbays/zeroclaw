# .envrc.mattermost.example — direnv template for Mattermost bot tokens
#
# Copy to .envrc.mattermost and fill in real tokens from mattermost-setup.sh.
#   cp .envrc.mattermost.example .envrc.mattermost
#
# To load automatically with direnv, add to your .envrc:
#   source_env .envrc.mattermost
#
# Or source manually:
#   source .envrc.mattermost
#
# IMPORTANT: Never commit .envrc.mattermost — it contains bot tokens.
# This .envrc.mattermost.example IS safe to commit (no real secrets).
#
# Tokens are generated by: ./scripts/mattermost-setup.sh

# ── Mattermost Server ─────────────────────────────────────────────
export MM_SITE_URL="http://localhost:8065"

# ── Bot Tokens ────────────────────────────────────────────────────
# Generated by scripts/mattermost-setup.sh
# Each token is a Mattermost Personal Access Token for the bot account.

export MM_TOKEN_SOKKA="replace_with_sokka_bot_token"
export MM_TOKEN_TOPH="replace_with_toph_bot_token"
export MM_TOKEN_AZULA="replace_with_azula_bot_token"
export MM_TOKEN_IROH="replace_with_iroh_bot_token"
export MM_TOKEN_KATARA="replace_with_katara_bot_token"
export MM_TOKEN_AANG="replace_with_aang_bot_token"
