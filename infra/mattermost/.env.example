# Mattermost Hub — Environment Variables
#
# Copy to .env and fill in values before running docker compose.
#   cp .env.example .env
#
# IMPORTANT: Never commit .env — it contains credentials.
# This .env.example IS safe to commit (no real secrets).

# ── PostgreSQL ────────────────────────────────────────────────────
MM_POSTGRES_USER=mattermost
MM_POSTGRES_PASSWORD=change_this_strong_password
MM_POSTGRES_DB=mattermost

# ── Mattermost Server ─────────────────────────────────────────────
# URL where Mattermost will be accessible.
# For local dev: http://localhost:8065
# For production: https://mm.yourdomain.com
MM_SITE_URL=http://localhost:8065

# ── Initial Admin Account ─────────────────────────────────────────
# Used by mattermost-setup.sh to create team, bots, and channels.
# The script authenticates as this admin to provision resources.
MM_ADMIN_EMAIL=admin@example.com
MM_ADMIN_USERNAME=admin
MM_ADMIN_PASSWORD=change_this_admin_password
