# ZeroClaw — Iroh (Creative/Mentor Bot) config
# Usage: zeroclaw start --config config/iroh.toml

default_provider = "ollama"
default_model = "qwen3:14b"
api_key = "http://localhost:11434"
default_temperature = 0.8

tool_allowlist = ["memory_store", "memory_recall", "ask_user"]

[identity]
format = "aieos"
aieos_path = "modes/iroh/identity.json"

[memory]
backend = "sqlite"
sqlite_path = "~/.zeroclaw/iroh.db"

[channels_config]
cli = false

[channels_config.mattermost]
url = "http://localhost:8065"
bot_token = "REPLACE_WITH_MM_TOKEN_IROH"
allowed_users = ["*"]
thread_replies = true
mention_only = true

[team]
human_username = "REPLACE_WITH_YOUR_MATTERMOST_USERNAME"

[[team.bots]]
username = "sokka"
role = "Dev"
description = "Code generation, debugging, file operations, shell commands"

[[team.bots]]
username = "katara"
role = "PM"
description = "Linear issues, project status, backlog grooming, standups"

[[team.bots]]
username = "toph"
role = "DevOps"
description = "Deployments, CI/CD, Docker, infrastructure, monitoring"

[[team.bots]]
username = "azula"
role = "Security"
description = "Code review, vulnerability scanning, security audit"

[[team.bots]]
username = "iroh"
role = "Creative"
description = "Brainstorming, architecture ideation, team memory, lateral thinking"

[[team.bots]]
username = "aang"
role = "Coordinator"
description = "Routes work, big picture, cross-team coordination"
