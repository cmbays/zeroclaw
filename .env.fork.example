# ZeroClaw Fork — docker-compose.fork.yml environment variables
#
# Copy to .env.fork and fill in real values:
#   cp .env.fork.example .env.fork
#
# IMPORTANT: Slack and Linear secrets belong in config/config.toml, not here.
# Only variables consumed by docker-compose.fork.yml directly live in this file.

# ── Cloudflare Tunnel ──────────────────────────────────────────────────────────
#
# Token for the cloudflared tunnel service.
# Create a tunnel at: Cloudflare Zero Trust → Networks → Tunnels → Add a tunnel
#   1. Choose "Cloudflared" connector type.
#   2. Copy the tunnel token shown during setup — paste it below.
#   3. In the tunnel settings, add a Public Hostname:
#        Subdomain: webhooks.<your-domain>
#        Service:   http://zeroclaw-fork:8080
#      (zeroclaw-fork is the container_name; 8080 must match [linear].webhook_port)
CLOUDFLARE_TUNNEL_TOKEN=REPLACE_ME
